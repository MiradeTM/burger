<li>
    {{name}}
    {{#if devour}}
    <button class="change-devour" data-id="{{id}}" data-newdevour="{{devour}}">
        Burger Time!
    </button>
    {{/if}}

    <button class="delete-burger" data-id="{{this.id}}">Burg, Bye!</button>
</li>